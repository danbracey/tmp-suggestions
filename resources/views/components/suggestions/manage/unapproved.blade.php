<a href="{{route('suggestion.approve', $suggestion->id)}}" class="btn btn-pill btn-success">Approve</a>
<a href="{{route('suggestion.deny', $suggestion->id)}}" class="btn btn-pill btn-danger">Deny</a>
