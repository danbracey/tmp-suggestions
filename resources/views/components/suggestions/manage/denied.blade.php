<a href="{{route('suggestion.reopen', $suggestion->id)}}" class="btn btn-pill btn-secondary">Re-open</a>
<a href="{{route('suggestion.deny', $suggestion->id)}}" class="btn btn-pill btn-danger">Deny</a>
